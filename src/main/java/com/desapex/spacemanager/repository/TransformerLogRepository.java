package com.desapex.spacemanager.repository;

import com.desapex.spacemanager.domain.TransformerLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TransformerLogRepository extends JpaRepository<TransformerLog, Long> {
}
